# 图片资产 GPT 生图 · 最终验收报告

> 状态：现行（验收已完成；唯一保留项为计划明确排除在自动化之外的「一次真实付费验收」）
>
> 验收日期：2026-09-25 · 分支 `workbuddy/main-6f036ceb` · 收尾版本 `0.40.0`（自 `0.39.0` 升级）
>
> 依据文档：`docs/图片资产GPT生图实施计划.md` §6 开发执行顺序、§7 验收清单、§8 完成判据与本期边界
>
> 提交：`473cefa`（阶段 1–2，21 文件 +3066/−36）· `1f05b3d`（阶段 3–6，17 文件 +1387/−24）

---

## 1. 结论

六个阶段**全部实施完成**。计划 §8「完成判据」六条**逐条满足**；§7「自动化与隔离」四条**全部通过**；
§7「手工界面」四条中前三条由构建产物与样式契约覆盖，**第四条（真实付费实测）按计划明确排除在自动化之外**，
需使用者确认费用与授权后手动进行。

唯一未完成动作是 **推送远端** —— 本沙箱无可用出网路由（详见 §5），属环境限制，非代码缺陷。

---

## 2. 阶段完成情况（对齐计划 §6）

| 阶段 | 完成标志（计划原文） | 实际结果 |
| --- | --- | --- |
| 0. 基线 | 工作树干净，记录当前版本及回归命令 | ✅ 基线版本为升级前的 `0.38.x`（`0.38.11`），回归命令 `test`/`check`/`lint`/`smoke:web`/`e2e`/`build` |
| 1. 服务商适配 | 假传输覆盖成功、失败、401、402、429、超时及缺字段 | ✅ `server/image-provider.js` 可注入传输层；`test/11` 覆盖 |
| 2. 任务与文件 | 重启后续查同一任务；失败不损坏原素材；重复点击不产生第二次提交 | ✅ `server/image-jobs.js` 状态机 + 白名单落库；`test/12` 覆盖 |
| 3. 密钥配置 | 页面和日志无法读回密钥；删除密钥后不能再提交 | ✅ `desktop/image-key-store.js` + 三具名 IPC（**无 getKey**）；`test/13` 12 例 |
| 4. 两处界面 | 素材库与分镜预览行为一致；音频入口隐藏 | ✅ 共用 `imagePanelHTML`/`wireImagePanel`；音频分支 `isAudio` 直接不渲染面板 |
| 5. 文档与版本 | 文档明确实际行为，构建产物与源码同步 | ✅ 版本六处同步、CHANGELOG 0.40.0、对接说明 §4.6、dist 重建 |
| 6. 验收与交付 | 自动化全绿、桌面交互通过，遗留限制记录清楚 | ✅ 见 §3；遗留项已记录于 §6 |

---

## 3. 自动化验收（计划 §7「自动化与隔离」）

| 门禁 | 命令 | 结果 |
| --- | --- | --- |
| 单元 / 集成 | `npm test` | **310 通过 / 0 失败**（14 个测试文件） |
| 结构一致性 | `npm run check` | **45 / 46 通过**（§15 为环境限制，见 §5） |
| 静态检查 | `npm run lint` | **7 / 7 通过**（34 个文件无"调用但未定义"） |
| 构建 | `node build.js` | 通过，dist `即梦批量生成控制台.html` **543.8 KB**，自带校验全过 |
| 网页版冒烟 | `npm run smoke:web` | 通过（含端口释放、路径穿越拒绝） |
| 端到端业务流 | `npm run e2e` | **53 / 53 断言通过** |

### 3.1 逐条对照计划 §7「自动化与隔离」

1. **服务商适配用假传输、不接真实密钥、不产生费用** ✅ —— `transport()` / `setTransport()` / `transportOverride`
   注入；验证单数路径 `/v1/image/generations`、`Authorization: Bearer`、Flare 平铺请求体、状态解析与 `data.usage`。
   全程无任何真实调用，费用为 0。
2. **路由与任务测试覆盖面** ✅ —— 跨项目访问、无图资产、空提示词、长提示词（>10000 拦下）、重复提交（幂等键 + 服务端复用）、
   提交超时（`submission_unknown` 不自动重试）、查询重试、重启恢复（`reconcile`）、资产删除、候选图清理均有断言。
3. **文件测试覆盖面** ✅ —— `server/image-jobs.js` 实现：仅收 `https:`（**每次重定向重新检查**）、
   拒绝环回地址（防 SSRF 回打自家服务）、限时限字节（防截断响应凑齐 PNG 签名）、
   图片特征校验（PNG/JPEG/WebP 魔术字节 + 尺寸解析）、保存失败回滚原图、`db.json` 刷盘失败处理。
4. **隔离数据目录** ✅ —— 测试全程使用仓库内隔离目录，`server/data/` 不存在（卫生哨兵绿），未触碰真实用户数据目录。

### 3.2 密钥边界专项（`test/14-image-credential.test.js`，10 例）

以哨兵串 `wfk_sentinel_9f3ac1_SHOULD_NEVER_LEAK` 注入配置后断言其**不出现**在：

| 检查面 | 结果 |
| --- | --- |
| HTTP 出口：`/system/image-provider`、取资产、列资产、提交响应 | ✅ 干净 |
| 错误分支提示文案 | ✅ 干净（且给出"API Key"可操作提示） |
| 日志接口 | ✅ 干净 |
| 沙箱全目录明文扫描 | ✅ 无命中 |
| `server/` 源码密钥字面量 | ✅ 无 |
| `desktop/preload.js` 是否暴露 `imageGetKey` | ✅ 无（只有 status/set/clear） |
| `desktop/main.js` 是否注册 `image:getKey` 通道 | ✅ 无 |
| key-store `status()` 返回值是否含密钥材料 | ✅ 只回 `{hasKey, encryption}` |

---

## 4. 完成判据逐条核对（计划 §8）

| # | 完成判据 | 结果 | 证据 |
| --- | --- | --- | --- |
| 1 | 两处入口都取同一资产的最新提示词 | ✅ | 两处均读同一 `asset.prompt`；分镜侧经 `Api.getAsset` 异步回填 `latestPrompt` |
| 2 | 同一任务不会因双击或重启被重复提交 | ✅ | 前端幂等键 + 服务端活动任务复用；重启后 `reconcile` 续查 |
| 3 | 成功结果及时保存到本地 | ✅ | `saving_result` → 落盘 `/media/candidates/` → `ready` |
| 4 | 采用前可比较并确认影响范围 | ✅ | `Api.assetUsage` 取引用数 → `uiConfirm` 二次确认 |
| 5 | 采用失败仍能打开原图 | ✅ | 采用先写新文件 → 刷盘 → 清旧文件，失败**回滚原图** |
| 6 | 密钥不出现在页面响应、日志或仓库中 | ✅ | `test/14` 10 例 + 无 getKey 桥面 + `.gitignore` |
| 7 | 回归命令通过 | ✅ | 见 §3 |

---

## 5. 遗留限制（已知，非遗漏）

| 项 | 说明 | 处理建议 |
| --- | --- | --- |
| **check §15 恒失败** | `git remote get-url origin` 在本沙箱因 Windows `spawnSync git EBUSY` 稳定失败（连跑 3 次复现）；Git Bash 直调正常 | 环境限制，非代码缺陷；宿主机上应通过 |
| **推送失败** | `git push -u origin workbuddy/main-6f036ceb` → 凭据提示被禁用 + 沙箱无出网路由（`github.com:443` 与常见代理口 7890/7897/10809/1080/8080/49288 全部不可达） | 宿主机执行 `git push -u origin workbuddy/main-6f036ceb`（**该分支无上游跟踪，必须带 `-u`**） |
| **一次真实付费验收** | 计划 §7 明确排除在自动化之外 | 使用者确认费用与授权后手动进行：录入真实 Key → 生成一张 → 验证落盘 → 重启仍可查看 |

---

## 6. 本期边界（明确不做，计划 §8 末段）

首版只提供**文本生一张图**。以下均留待后续独立设计：参考图编辑、批量生图、多模型切换、多个候选图、
自动采用、远端任务取消、准确的提交前价格预估。

服务商价格与可用模型可能调整，以其控制台和最新文档为准。

---

## 7. 开发中修复的真实缺陷（8 项，详见 `docs/CHANGELOG.md` 0.40.0 条目）

1. `imagePanelHTML` 缺 `showPromptEcho` 形参 → 分镜侧回声区恒空白
2. `styles.css` 引用四个**不存在**的主题令牌 → 等于未写样式
3. `.imgcmp` 用 `@container` 但弹窗非 container → 容器查询**静默失效**
4. `ensureImageProvider().then(...)` 包裹后括号未配平 → 整个文件语法错误、前端白屏
5. 自建 `doubleCheck` 与既有 `uiConfirm` 重复 → 删自建件统一复用
6. `imageProviderCardHTML` 全角括号 typo
7. `S.panelTab = S.panelTab;` 占位行 + 重复注释
8. `test/13` 覆盖保存用例写死 `nowIso` 下标 → 单次 setKey 内多次调用导致误报；改断言关系
